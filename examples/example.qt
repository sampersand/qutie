drive = func(dist, song){
   if ( dist == 0 ){
      __debug "Vroom Vroom!"
   } else {
      __debug "I drove "
      __debug dist
      __debug "miles listening to"
   }
};
drive(1, "Wheels on the bus")